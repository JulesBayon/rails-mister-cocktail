<h1>Mister cocktaiiiiiil, sans alcool la fête est plus fun!</h1>

<% @cocktails.each do |cocktail| %>
  <h2><%= link_to cocktail.name, cocktail_path(cocktail) %></h2>
<% end %>
<%= link_to 'Add a cocktail', new_cocktail_path  %>

<%= cl_image_tag("THE_IMAGE_ID_FROM_LIBRARY", width: 400, height: 150, crop: :fill) %>
<%= cl_image_tag("IMAGE_WITH_FACE_ID", width: 150, height: 150, crop: :thumb, gravity: :face) %>
